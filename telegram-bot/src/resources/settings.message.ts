export const settingsMessage = {
  main() {
    return `
⚙️ <b>ГЛОБАЛЬНЫЕ НАСТРОЙКИ</b>

Выберите категорию для изменения конфигурации:

🔐 <b>Безопасность</b> — настройки приватности, режим "Стелс" и скрытие профиля.
🎨 <b>Интерфейс</b> — валюта, часовой пояс и компактный режим.
🔔 <b>Уведомления</b> — управление оповещениями о гонках и новостях.

<i>Нажмите на кнопку ниже, чтобы открыть нужный раздел.</i>
`.trim();
  },

  interface(data: { currency: string; timezone: number; isCompact: boolean }) {
    const compactStatus = data.isCompact ? "Включен" : "Выключен";
    const tzSign = data.timezone >= 0 ? "+" : "";

    return `
🎨 <b>НАСТРОЙКИ ИНТЕРФЕЙСА</b>

Настройте внешний вид бота под себя.

💵 <b>Валюта:</b> ${data.currency}
🕒 <b>Часовой пояс:</b> МСК${tzSign}${data.timezone}
📱 <b>Компактный вид:</b> ${compactStatus}

<i>Нажимайте кнопки ниже, чтобы изменить параметры.</i>
`.trim();
  },
};
